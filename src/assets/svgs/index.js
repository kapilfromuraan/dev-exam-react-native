import React from 'react';
import ChevronBack from './chevron-back.svg';
import ChevronForward from './chevron-forward.svg';

export const ChevronBackIcon = ({ width, height }) => <ChevronBack width={width} height={height} />
export const ChevronForwardIcon = ({ width, height }) => <ChevronForward width={width} height={height} />