import React from 'react';
import ChevronBack from './chevron-back.svg';
import ChevronBackLight from './chevron-back-light.svg';
import People from './people.svg';
import PeopleLight from './people-light.svg';
import Code from './code.svg';
import Sun from './sun.svg';
import Moon from './moon.svg';

export const ChevronBackIcon = ({ width, height }) => <ChevronBack width={width} height={height} />
export const ChevronBackLightIcon = ({ width, height }) => <ChevronBackLight width={width} height={height} />
export const PeopleIcon = ({ width, height }) => <People width={width} height={height} />
export const PeopleLightIcon = ({ width, height }) => <PeopleLight width={width} height={height} />
export const CodeIcon = ({ width, height }) => <Code width={width} height={height} />
export const SunIcon = ({ width, height }) => <Sun width={width} height={height} />
export const MoonIcon = ({ width, height }) => <Moon width={width} height={height} />